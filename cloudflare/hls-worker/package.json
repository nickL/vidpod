{
  "name": "vidpod-hls-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --env=",
    "deploy:dev": "wrangler deploy --env dev",
    "dev": "wrangler dev --env dev"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
